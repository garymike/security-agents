name: Security

# Dogfoods the Tier-1 platform (garymike/security-workflows v1.6.0) on this repo.
# Pinned to the commit v1.6.0 resolves to, so a moved tag cannot change what runs here.

on:
  push:
    branches: ["**"]
  pull_request:
  schedule:
    - cron: '0 8 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  scan:
    permissions:
      contents: read
      packages: read
    uses: garymike/security-workflows/.github/workflows/security-scan.yml@8a5a69ab1ef4903bb6f281f8318770012fa6167c # v1.6.0

  gha-security:
    permissions:
      contents: read
      packages: read
    uses: garymike/security-workflows/.github/workflows/gha-security.yml@8a5a69ab1ef4903bb6f281f8318770012fa6167c # v1.6.0

  sast:
    permissions:
      contents: read
      packages: read
      security-events: write
    uses: garymike/security-workflows/.github/workflows/sast.yml@8a5a69ab1ef4903bb6f281f8318770012fa6167c # v1.6.0

  iac-security:
    permissions:
      contents: read
      packages: read
      security-events: write
    uses: garymike/security-workflows/.github/workflows/iac-security.yml@8a5a69ab1ef4903bb6f281f8318770012fa6167c # v1.6.0

  audit:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    permissions:
      contents: read
    uses: garymike/security-workflows/.github/workflows/security-audit.yml@8a5a69ab1ef4903bb6f281f8318770012fa6167c # v1.6.0
